package com.kousoyu.drift.ui.theme

import androidx.compose.ui.graphics.Color

// ── Night Mode (Futuristic Tech) ──────────────────────────────────────────
// True OLED black background — battery-saving & looks premium on AMOLED screens
val DarkBackground    = Color(0xFF000000)
val DarkSurface       = Color(0xFF0D0D0D) // slightly elevated cards
val DarkSurfaceVariant= Color(0xFF1A1A1A) // chips, nav bar
val DarkOnBackground  = Color(0xFFFFFFFF)
val DarkOnSurface     = Color(0xFFE0E0E0)
val DarkOnSurfaceVar  = Color(0xFF9E9E9E) // secondary labels
val DriftAccent       = Color(0xFF7C6EFF) // soft indigo-violet — the Drift signature color
val DriftAccentDark   = Color(0xFF4A3FE0)
val DarkOutline       = Color(0xFF2A2A2A) // thin dividers, zero visual noise

// ── Day Mode (Clean Paper) ────────────────────────────────────────────────
val LightBackground   = Color(0xFFFFFFFF)
val LightSurface      = Color(0xFFF8F8F8)
val LightSurfaceVariant = Color(0xFFEEEEEE)
val LightOnBackground = Color(0xFF0A0A0A)
val LightOnSurface    = Color(0xFF1C1C1C)
val LightOnSurfaceVar = Color(0xFF6B6B6B)
val LightAccent       = Color(0xFF5548D9) // same hue, deeper for light mode contrast
val LightOutline      = Color(0xFFE0E0E0)